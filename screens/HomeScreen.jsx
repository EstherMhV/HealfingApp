import { View, Text, ScrollView } from "react-native";

export default function Home({ navigation }) {
  return (
    <ScrollView>
      <View>
        <Text>page Home</Text>
      </View>
    </ScrollView>
  );
}
