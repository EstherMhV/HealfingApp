import { View, Text, ScrollView } from "react-native";

const Home = ({ navigation }) => {
  return (
    <ScrollView>
      <View>
        <Text>page Home</Text>
      </View>
    </ScrollView>
  );
};

export default Home;
